# [[.vscode/settings.json]]

## Abhaengigkeiten
- Keine Python-Module; nur VS-Code-Workspace-Einstellung.

## Klassen
- Keine.

## Zweck
- Setzt den Standard-Interpreter auf die lokale `.venv`, damit Pylance `pygame` korrekt aufloesen kann.
